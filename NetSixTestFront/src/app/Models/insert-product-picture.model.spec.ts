import { InsertProductPicture } from './insert-product-picture.model';

describe('InsertProductPicture', () => {
  it('should create an instance', () => {
    expect(new InsertProductPicture()).toBeTruthy();
  });
});
