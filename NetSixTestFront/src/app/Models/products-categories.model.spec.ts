import { ProductsCategories } from './products-categories.model';

describe('ProductsCategories', () => {
  it('should create an instance', () => {
    expect(new ProductsCategories()).toBeTruthy();
  });
});
